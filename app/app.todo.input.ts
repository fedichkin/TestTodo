import { Component } from '@angular/core';
     
@Component({
    moduleId: module.id,
    selector: 'todo-input',
    templateUrl: 'template/todo.input.html' 
})
export class TodoInput { 
    task= '';
}